# Docker

The Compose stack lives at the repository root (`docker-compose.yml`) so
`docker compose up` works without extra flags. This directory is reserved for
supplementary Docker assets (e.g. database init scripts) as the project grows.

See [`docs/docker.md`](../docs/docker.md) for usage instructions.

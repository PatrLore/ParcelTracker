# Copy to .env and adjust for docker-compose.yml.
# These only configure the Postgres container itself; the backend's own
# database credentials are set in backend/config.yaml (database.user/password
# must match POSTGRES_USER/POSTGRES_PASSWORD below).

POSTGRES_DB=parcel_server
POSTGRES_USER=parcel_server
POSTGRES_PASSWORD=change-me
